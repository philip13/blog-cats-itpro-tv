<div class="bg-gray-100 grid lg:grid-cols-2 2xl:grid-cols-5">
  <div class="px-8 py-12 max-w-md mx-auto sm:max-w-xl lg:px-12 lg:py-24 lg:max-w-full xl:mr-0 2xl:col-span-2">
    <div class="xl:max-w-xl">
      <img class="mt-6 rounded-lg shadow-xl sm:mt-8 sm:h-64 sm:w-full sm:object-cover object-center lg:hidden" src="https://loremflickr.com/1200/800/cat" alt="Wellcome Cat" />
      <h1 class="mt-6 text-2xl font-bold text-gray-900 sm:mt-8 sm:text-4xl lg:text-3xl xl:text-4xl">
        Wellcome to Cat Blog
        <br class="" />
        <span class="text-red-300">All of your Kitty.</span>
      </h1>
      <p class="mt-2 text-gray-600 sm:mt-4 sm:text-xl">
        Orci varius natoque penatibus et magnis dis parturient montes, nascetur ridiculus mus. Sed vel laoreet turpis.
      </p>
      <div class="mt-4 sm:mt-6">
        <!-- <a class="inline-block shadow-lg btn btn-pink" href="#"> READ MORE</a> -->
        <%= link_to 'READ MORE', @posts.last, :class => "inline-block shadow-lg btn btn-pink" %>
      </div>
    </div>
  </div>
  <div class="hidden relative lg:block 2xl:col-span-3">
    <img class="absolute inset-0 w-full h-full object-cover object-center" src="https://loremflickr.com/1200/800/cat" alt="Wellcome Cat"/>
  </div>
</div>

<div class="bg-gray-100 lg:bg-white">
  <div class="px-8 py-12 max-w-md mx-auto sm:max-w-xl lg:px-12 lg:py-24 lg:max-w-full xl:mr-0 grid gap-4 grid-cols-1 lg:grid-cols-2">
    <% @posts.each do |post| %>
    <div class="px-6 pb-4 border-solid border-2 border-gray-200 rounded-lg">
      <%= image_tag post.image.url, :class => "mt-6 rounded-lg shadow-xl sm:mt-8 sm:h-64 sm:w-full sm:object-cover object-center" %>
      
      <h1 class="mt-6 text-2xl font-bold text-gray-900 sm:mt-8 sm:text-4xl lg:text-3xl xl:text-4xl">
        <%= post.title%>
      </h1>
      <p class="mt-2 text-gray-600 sm:mt-4 sm:text-xl">
        <%= post.body.truncate(255, separator: ' ') %>
      </p>
      <div class="mt-4 sm:mt-6 md:flex md:justify-end ">
        <%= link_to 'READ MORE', post, :class => "inline-block shadow-lg btn btn-pink" %>
        <% if user_signed_in? %>
          <%= link_to 'Edit', edit_post_path(post), :class => "btn btn-blue mx-3" %>
          <%= link_to 'Destroy', post, :class => "inline-block  shadow-lg mt-3 md:mt-0 btn btn-outline-pink", method: :delete, data: { confirm: 'Are you sure?' } %>
        <%end%>
      </div>
    </div>
    <%end%>
  </div>
</div>


<%#= link_to 'New Post', new_post_path if user_signed_in? %>
